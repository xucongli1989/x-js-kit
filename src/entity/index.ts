import * as idCard from "./idCard"
import * as keyValue from "./keyValue"
import * as select from "./select"


export default {
    /**
     * 证件实体
     */
    idCard,
    /**
     * key/value实体
     */
    keyValue,
    /**
     * 下拉框相关实体
     */
    select
}