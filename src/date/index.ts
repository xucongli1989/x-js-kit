/* eslint-disable import/no-default-export */
import * as format from "./format"
import * as convert from "./convert"

export default {
    /**
     * 格式化相关
     */
    format,
    /**
     * 转换相关
     */
    convert
}
