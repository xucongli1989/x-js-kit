/* eslint-disable import/no-default-export */
import stopWatch from "./stopWatch"

export default {
    /**
     * 计时器（请使用大写字母开头的）
     */
    stopWatch,
    /**
     * 计时器
     */
    StopWatch: stopWatch
}
