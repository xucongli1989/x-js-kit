/**
 * 判断是否为email
 */
export declare const isEmail: (val: string) => boolean;
/**
 * 判断是否全为英文字符（a-zA-Z）
 */
export declare const isAllEnglish: (val: string) => boolean;
/**
 * 判断是否全为数字字符（0-9）
 */
export declare const isNumber: (val: string) => boolean;
